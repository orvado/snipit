pystray>=0.19.5
Pillow>=10.0
keyboard>=0.13.5

# Platform extras (installed automatically as needed):
# - macOS: pyobjc-framework-Cocoa is pulled by pystray when needed for menu-bar
# - Linux: gir1.2-appindicator3 may be required at OS level for tray
